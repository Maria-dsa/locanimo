{% extends 'base.html.twig' %}

{% block title %}Mettre à jour l'annonce{% endblock %}

{% block body %}
    <div class="container flex text-center">
    <h1><i class="bi bi-gear me-2"></i>Mettre à jour l'annonce</h1>

    {{ include('animal/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_animal_mine') }}">back to list</a>

    {{ include('animal/_delete_form.html.twig') }}
    </div>
{% endblock %}
