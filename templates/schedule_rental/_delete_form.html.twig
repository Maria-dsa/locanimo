<form method="post" action="{{ path('app_schedule_rental_delete', {'id': schedule_rental.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ schedule_rental.id) }}">
    <button class="btn">Delete</button>
</form>
