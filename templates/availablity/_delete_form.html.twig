<form method="post" action="{{ path('app_availablity_delete', {'id': availablity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ availablity.id) }}">
    <button class="btn">Delete</button>
</form>
